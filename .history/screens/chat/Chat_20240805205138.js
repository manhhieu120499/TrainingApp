import { View, Text } from 'react-native';
import { colors, fontSize } from '../../constants';

function Chat(props) {
	return (
		<View>
			<Text>This is chat room</Text>
		</View>
	);
}

export default Chat;
